#include <stdio.h>
/**
 *main -Entry point
 *
 *Description:uses puts to output
 *
 *"programming is like building a multilingual puzzle"
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
    char *holbertonschool = " \"programming is like building a multilingual puzzle
    puts(holbertonStr);
    return(0);
}